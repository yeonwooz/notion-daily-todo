# 2026-06-20: 이월(carryover) 제거 — "오늘의 할일"만 생성

## 배경

사용자 보고: 아침 todo 알림(haru-bot)에 **오늘 일정과 함께 며칠 전 일정**이 같이 옴.
명세: **캘린더는 KST 오늘 날짜**, **Notion은 오늘 날짜/오늘 요일의 반복 할일**만
"오늘의 할일"이어야 한다.

haru-bot 아침 알림은 자체 수집 없이 notion-daily-todo가 만든 `YYYY-MM-DD TODO`
페이지를 그대로 읽어 보낸다 → 근본 원인은 이 페이지 생성 쪽.

## 진단

- 캘린더 **수집** 단계는 이미 KST 오늘만 가져옴 (6/16 `_event_on_kst_day`).
- **이월(carryover)** 이 원인: 전날 페이지의 미완료 항목을 다음 날로 이월했는데,
  캘린더 일정(`[14:00] 회의`)·요일 반복("화요일마다")처럼 날짜·요일에 매인 항목이
  체크 안 된 채 남으면 그 날 외에는 재생성되지 않아 **매일 계속 이월** →
  "며칠 전 일정"으로 누적됐다.

## 결정

이월 자체를 제거. 매일 페이지는 "오늘의 할일"만 담는다:
템플릿 고정 task + 오늘 KST 캘린더 일정 + 오늘 날짜/요일 반복 할 일.

(전날 못한 일을 가져오는 동작이 필요하면, 자동 이월 대신 사용자가 "내일로 미룰 것"을
선택하는 인터랙션으로 별도 설계 — 추후 검토.)

## 변경 (`create_daily_todo.py`)

- `build_page`에서 carryover 파라미터·처리 전부 제거 → `build_page(today,
  parent_page_id, template_children, classified)`. 템플릿 순회하며 카테고리별
  classified만 추가(중복 dedup 유지).
- 미사용이 된 `find_yesterday_page`, `extract_unchecked_by_category` 삭제.
- `main`에서 전날 페이지 검색/이월 수집 블록 제거, `yesterday_str` 제거.
- 모듈 docstring 콘텐츠 소스 목록에서 "전날 TODO 페이지 이월" 항목 삭제.

## 검증

- `py_compile` OK, carryover 잔여 참조 없음.

## 영향 범위

- 이미 생성된 오늘(6/20) 페이지에는 적용 안 됨(아침 알림 이미 발송). **다음 생성
  (6/21 06:00 KST)부터** 오늘 항목만 깔끔하게 생성.
- haru-bot 코드 변경 없음 — 페이지만 정상화되면 아침 알림도 자동 정상화.

## 관련

- 6/16 `2026-06-16_캘린더-종일일정-인접일-누수-수정.md` (수집 단계 KST 필터) —
  이번 건은 그 이후 단계인 **이월** 누수로 별개.
